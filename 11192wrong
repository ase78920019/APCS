#include <iostream>
#include <string.h>
#include <algorithm>
using namespace std;
int main()
{
	ios::sync_with_stdio(0);
	cin.tie(0);
	int a;
	string s;
	while(cin>>a){
		if(a==0)break;
		string s1;
		for(int i=0;i<(s.size())/a+1;i++){
			s1=s.substr(a*i,a);
			int *p=s[a-1];
			cout<<s1;
		}
		cout<<'\n';
	}
	
	
	return 0;
}
