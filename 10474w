#include<iostream>
using namespace std;

int a[10005],b[10005];

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	int n,q,o=1,flag=0;
	while(cin>>n>>q){
		if(n==0&&q==0)break;
		cout<<"CASE# "<<o<<":\n";
		for(int i=0;i<n;i++)
			cin>>a[i];
		for(int i=0;i<q;i++)
			cin>>b[i];
		
		int m=n;
		for(int j=0;j<n-1;j++){
		  for(int i=m-1;i>=0;i--){
			if(a[i]>a[i+1])swap(a[i],a[i+1]);
	   	  }
	   	  m--;
		}
		for(int i=0;i<q;i++){
		  for(int j=0;j<n;j++){
			if(a[j]==b[i]){
				cout<<b[i]<<" found at "<<++j<<'\n';
				break;
			}
			flag++;
			if(flag==n)
				cout<<b[i]<<" not found\n";
		  }
		  flag=0;
		}
		o++;
	}
	
	return 0;
}
