#include <stdio.h>
#include <string.h>
using namespace std;
int main()
{
	int n;
	int a,b;
	scanf("%d",&n);
	while(n--&&scanf("%d %d",&a,&b)){
		int ary[a][b];
		int c,i,j;
		char s[55];
		for(i=0;i<a;i++)
		  for(j=0;j<b;j++)
			scanf("%d",&ary[i][j]);
		scanf("%d",&c);
		for(int k=0;k<c;k++){
			gets(s);
			getchar();
			for(j=0;j<b;i++)
		  	  for(i=0;i<a;j++)
		  	  	if(s[0]==a[i][j]){
		  	  		for(int o=0;o<8;o++)
		  				a[i][j]
					}
		}
	}
	
	
	return 0;
}



#include <iostream>
#include <stdio.h>
using namespace std;

char ary[55][55],ary2[55];
int N,m,n,a;

void Aa(char aa[]){
	for(int i=0;aa[i];i++)
	  if(aa[i]>='A'&&aa[i]<='Z')aa[i]+=32;
}

bool test (int i,int j){
	int o;
	for(o=1;j-o>=0&&ary2[o]==ary[i][j-o];o++){
		if(ary2[o]=='\0')
			return true;
	}
	for(o=1;j+o<n&&ary2[o]==ary[i][j+o];o++){
		if(ary2[o]=='\0')
			return true;
	}
	for(o=1;i-o>=0&&ary2[o]==ary[i-o][j];o++){
		if(ary2[o]=='\0')
			return true;
	}
	for(o=1;i+o<m&&ary2[o]==ary[i+o][j];o++){
		if(ary2[o]=='\0')
			return true;
	}
	for(o=1;j-o>=0&&i-o>=0&&ary2[o]==ary[i-o][j-o];o++){
		if(ary2[o]=='\0')
			return true;
	}
	for(o=1;j-o>=0&&i+o<m&&ary2[o]==ary[i+o][j-o];o++){
		if(ary2[o]=='\0')
			return true;
	}
	for(o=1;i-o>=0&&j+o<n&&ary2[o]==ary[i-o][j+o];o++){
		if(ary2[o]=='\0')
			return true;
	}
	for(o=1;i+o<m&&j+o<n&&ary2[o]==ary[i+o][j+o];o++){
		if(ary2[o]=='\0')
			return true;
	}
	return false;
}
int main()
{
	cin>>N;
	while(N--){
		cin>>m>>n;
		for(int i=0;i<m;i++){
			scanf("%s",ary[i]);
			Aa(ary[i]);
		}
		cin>>a;
		while(a--){
			scanf("%s",ary2);
			Aa(ary2);
			
			int i,j,flag=0;
			for(i=0;i<m&&!flag;i++)
			  for(j=0;j<n&&!flag;j++)
				if(ary[i][j] == ary2[0] &&test(i,j)){
					printf("%d %d\n",i++,j++);
					flag=1;
				}
		}
		if(N>=1)printf("\n");
	}
	
	return 0;
}
