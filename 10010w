#include <stdio.h>
#include <string.h>
using namespace std;
int main()
{
	int n;
	int a,b;
	scanf("%d",&n);
	while(n--&&scanf("%d %d",&a,&b)){
		int ary[a][b];
		int c,i,j;
		char s[55];
		for(i=0;i<a;i++)
		  for(j=0;j<b;j++)
			scanf("%d",&ary[i][j]);
		scanf("%d",&c);
		for(int k=0;k<c;k++){
			gets(s);
			getchar();
			for(j=0;j<b;i++)
		  	  for(i=0;i<a;j++)
		  	  	if(s[0]==a[i][j]){
		  	  		for(int o=0;o<8;o++)
		  				a[i][j]
					}
		}
	}
	
	
	return 0;
}
