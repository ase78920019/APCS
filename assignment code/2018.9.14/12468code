#include <iostream>
#include <cmath>
using namespace std;

int main(void) {
	ios::sync_with_stdio(0);
	cin.tie(0);
	int a,b;
	while(cin>>a>>b){
		if(a==-1&&b==-1)break;
		if(abs(a-b)>50&&a>b)a-=100;
		else if(abs(a-b)>50&&a<b)b-=100;
		cout<<abs(a-b)<<"\n";
	}

	return 0;
}
